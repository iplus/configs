(setq clojure-plus-packages '((parinfer-mode :location (recipe :fetcher github
                                                               :repo "edpaget/parinfer-mode"))))

(defun clojure-plus/init-parinfer-mode ()
  ;; (use-package parinfer-mode
  ;;              ;; :defer t
  ;;              ;; :mode ("\\.php\\'" . php-mode)
  ;;              :init (progn
  ;;                      (add-hook 'clojure-mode-hook #'parinfer-mode)) )
  )
